import 'package:flutter/material.dart';
import 'dummy_page.dart';

class ProfilePage extends StatelessWidget {
  const ProfilePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const DummyPage(title: 'Profil');
  }
}
